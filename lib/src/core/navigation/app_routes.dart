/// Enum, that contains title with path required for configurerRouter
enum AppRoutes {
  // Main routes
  home,
  categories,
  cart,
  favorites,
  profile,

  // home sub routes
  homeProduct,
  homeCategory,
  homeCategoryProduct,
  search,
  searchProduct,

  // category sub routes
  category,
  categoryProduct,

  // brand sub routes
  brand,
  brandProducts,

  //cart sub routes
  cartProducts,
  checkout,
  checkoutAddress,


  // favorite sub routes
  favoritesProducts,

  // profile sub routes
  about,
  orders,
  address,
  accountInfo,
  order;
}
