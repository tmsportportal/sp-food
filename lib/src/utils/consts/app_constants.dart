import 'package:flutter/cupertino.dart';

@immutable
class AppConstants {
  const AppConstants._();

  static const smallMobileBreakpoint = 500.0;
  static const mobileBreakpoint = 860.0;
  static const wideMobileBreakpoint = 920.0;

  static const mobileMaxWidth = 750.0;

  static const webBreakpoint = 1400.0;

  static const appBarWidth = 1200.0;
  static const footerWidth = 1200.0;

  static const requiresAuthToken = 'requiresAuthToken';

  static const elevatedButtonHeight = 50.0;
  static const gridProductExtend = 360.0;
}
